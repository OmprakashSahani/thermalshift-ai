services:
  - type: web
    name: thermalshift-ai
    runtime: python
    plan: free
    buildCommand: python -m pip install .
    startCommand: python -m uvicorn thermalshift.web.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /healthz
